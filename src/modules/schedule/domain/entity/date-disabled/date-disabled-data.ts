export interface CreateDateDisabled {
    id?: string 
    date: Date
    service_id: string
    created_at?: Date
    updated_at?: Date
}

export interface DateDisabledData {
    id: string 
    date: Date
    service_id: string
    created_at?: Date
    updated_at?: Date
}